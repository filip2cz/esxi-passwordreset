#!/usr/bin/expect -f

# Spustí abuild-keygen s příslušnými parametry
spawn abuild-keygen -i -a

# Čeká na výzvu pro zadání souboru pro uložení klíče a pošle cestu nebo použije výchozí
expect "Enter file in which to save the key"
send "\r"

# Čeká na konec interakce
expect eof
